FROM python:3.10-slim

WORKDIR /app
COPY requirements.txt pyproject.toml ./
RUN pip install --no-cache-dir fastapi uvicorn[standard] pydantic pydantic-settings aiosqlite websockets jinja2
COPY app/ app/

EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
